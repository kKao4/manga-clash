import { createRipples } from "react-ripples";

export const MyRipples = createRipples({
  color: "rgba(255,255,255,0.4)",
  during: 1200,
})

export const OrderRipples = createRipples({
  color: "rgba(64,154,136,0.2)",
  during: 800
})